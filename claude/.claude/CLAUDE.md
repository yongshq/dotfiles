# Personal default rules

My shared, agent-agnostic workflow rules live in `AGENTS.md`. The line below
imports them so Claude Code loads them as global rules — edit `AGENTS.md`, not
this file.

@~/dotfiles/AGENTS.md
